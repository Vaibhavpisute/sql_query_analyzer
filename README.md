# sql_query_analyzer
SQL Query Cost Analyzer – FastAPI service that detects SQL performance issues, estimates execution costs, and provides optimization tips. Implements async batch processing with asyncio and thread-safe LRU caching (90% faster responses). Identifies anti-patterns like missing WHERE clauses and leading wildcards to catch issues early.
